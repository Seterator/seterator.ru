@model Participant
@{
    ViewData["Title"] = "Become";
    
}

<h1>Become</h1>

<h4>Participant</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @Model.CompetitionGuid
        <form asp-action="Become">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="submit" value="Записаться" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

